import { Piece } from './classes/Piece';
import { Game } from './classes/Game';

let game = new Game();
game.update();
game.grabInputEvents();
// let piece: Piece | undefined = game.board.getPiece(2, 1);
